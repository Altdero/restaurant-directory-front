# Architecture

## Data layer: one interface, two implementations

`httpResource()` returns `HttpResourceRef<T>`; TanStack Query returns `CreateQueryResult`. Neither type may leak into a component. `core/interfaces/` defines the shapes both implementations satisfy:

```ts
export interface AsyncResource<T> {
  readonly value: Signal<T | undefined>;
  readonly isLoading: Signal<boolean>;
  readonly error: Signal<ApiError | undefined>;
  reload(): void;
}

export interface AsyncMutation<TInput, TResult> {
  readonly isPending: Signal<boolean>;
  readonly error: Signal<ApiError | undefined>;
  mutate(input: TInput): Promise<TResult>;
}
```

Each resource gets one interface (`RestaurantDataService`, `CategoryDataService`, `ReviewDataService`, `MenuItemDataService`, `FavoriteDataService`) built from these two primitives, plus an `InjectionToken`:

```ts
export const RESTAURANT_DATA = new InjectionToken<RestaurantDataService>('RESTAURANT_DATA');
```

`core/services/http-resource/` and `core/services/tanstack/` each implement every interface. `app.config.ts` binds all five tokens to one family, chosen by `environment.dataLayer: 'http-resource' | 'tanstack'`:

```ts
providers: [
  environment.dataLayer === 'tanstack'
    ? provideTanStackDataLayer()
    : provideHttpResourceDataLayer(),
];
```

Components inject `inject(RESTAURANT_DATA)` and never learn which implementation they received. Adding a sixth resource means adding one interface and two implementations — no existing file changes.

**Both implementations ship in the browser bundle regardless of the switch — a known, accepted tradeoff, not an oversight.** `environment.dataLayer` is a plain runtime object property, not a build-time constant substituted via `--define` (unlike `NG_APP_API_BASE_URL`), so esbuild's dead-code elimination cannot prove which branch of the ternary above actually runs and keeps both. This pushed the production initial-bundle size from ~420 kB to ~627 kB (commit 8), which required raising `angular.json`'s `budgets[0].maximumWarning` from `500kB` to `650kB` — a deliberate, documented change, not a silenced warning. The whole point of this architecture is demonstrating two interchangeable, fully-implemented data layers side by side, so shipping both is consistent with that goal for a project at this stage. If bundle size becomes a real concern later, the upgrade path mirrors `NG_APP_API_BASE_URL`: promote `dataLayer` to a build-time `--define`'d global, so only the selected branch survives tree-shaking — an additive change, not a re-plan.

Both implementations reuse the same implementation-agnostic pieces: `ApiUrlBuilder` (query serialization), `ApiErrorMapper` (DRF error shapes → a discriminated `ApiError` union), the three pagination-envelope decoders, and the DTO↔model mappers that parse decimal strings and ISO timestamps. The only thing that differs between the two implementations is the fetching mechanism itself — which is also why both are tested against one shared contract suite rather than duplicated test files.

### `httpResource()` implementation — implemented and verified

`core/services/http-resource/http-resource.adapter.ts` holds the two pieces every one of the five resource services (`core/services/http-resource/*.http-resource.service.ts`) builds on:

- **`toAsyncResource()`** adapts an `httpResource()` ref to `AsyncResource<T>`. It takes a `ResourceLike<T>` — a structural subset of `HttpResourceRef<T>` (just `value`/`isLoading`/`error`/`reload`) — rather than `HttpResourceRef<T>` itself. `WritableResource.set`/`update` take `T` in a contravariant (input) position, so `HttpResourceRef<Restaurant>` (built with a `defaultValue`, used by `list()`/`mine()`) structurally fails to satisfy a parameter typed `HttpResourceRef<Restaurant | undefined>` (built without one, used by `byId()`) even though both are equally valid to _read_ — reading only the four members this adapter touches sidesteps that.
- **`createMutation()`** builds an `AsyncMutation<TInput, TResult>` from a one-shot `HttpClient` call (`firstValueFrom` + local `isPending`/`error` signals) for every `create()`/`update()`/`remove()`/`toggle()` method. Deliberately not `httpResource()`-based — mutations are imperative, one-shot calls, not reactive signal-driven queries.

**Verified finding that corrected an initial wrong assumption:** reading `@angular/core/fesm2022/_resource-chunk.mjs` suggested `resource()`'s `encapsulateResourceError` wraps any thrown non-`Error`-like value (no string `.name`/`.message`) in a `ResourceWrappedError`, putting the original on `.cause` — since `error.interceptor.ts` throws the mapped `ApiError` object directly (never an `Error` instance), the adapter was first written to unwrap `.cause`. A real `HttpTestingController` 404 response proved this wrong for `httpResource()` specifically: `resource.error()` holds the exact `ApiError` object, completely unwrapped, no `.cause` involved. The adapter now casts `resource.error()` straight through `unknown` to `ApiError | undefined` — the `Signal<Error | undefined>` type is misleading at runtime for this resource factory. Caught by writing the test first and getting a genuine failure, not by re-reading the source harder.

**Query mapping** (`core/utils/query-params.ts`, one `to*Params()` function per resource) and **pagination-envelope mapping** (`core/utils/pagination-mapper.ts`, `mapCountedPage`/`mapCursorPage`) are both implementation-agnostic and reused by the TanStack implementation below — only the fetching mechanism differs, per the section above.

**Cursor pagination's "load more" is not signal-driven.** `ReviewDataService.loadMore(url: string): Promise<CursorPage<Review>>` takes the API's own opaque `next`/`previous` URL verbatim and returns a plain `Promise` — never a `cursor` query param reconstructed from a `ReviewQuery`. Accumulating pages into one growing list is the calling feature page's job (commit 12), not the data layer's — the data layer's only responsibility is fetching and normalizing one page at a time.

**Testing `httpResource()`-backed resources requires two things beyond a normal `HttpTestingController` test**, both confirmed empirically, not assumed:

1. `TestBed.runInInjectionContext(() => service.list(query))` to call the resource-creating method — `httpResource()` is an `@initializerApiFunction` requiring an active injection context, same rule as `inject()`. Calling a method on an already-constructed service instance is not automatically in one.
2. `TestBed.tick()` after creation triggers the resource's initial effect and issues the HTTP request; after `req.flush(...)`, the signal write does **not** happen synchronously inside `flush()` — `ResourceImpl#loadEffect` (`_resource-chunk.mjs`) is an `async` method, and JS guarantees the code after any `await` runs on a microtask, never synchronously, regardless of how fast the awaited value resolves. `await TestBed.inject(ApplicationRef).whenStable()` (a real, documented Angular primitive for "wait for pending async work to settle" — not a bare `Promise.resolve()` guess) is required before asserting on the resolved state.

### TanStack Query implementation — implemented and verified

`core/services/tanstack/tanstack.adapter.ts` mirrors the httpResource adapter's shape (`toAsyncResource()`, plus `toAsyncMutation()` for writes) but the mechanics underneath are genuinely different, each one verified rather than assumed:

- **No error-wrapping to undo, unlike `httpResource()`.** TanStack Query does not run thrown values through anything like Angular's `encapsulateResourceError` — `query.error()` holds exactly what `error.interceptor.ts` threw. The only real adaptation is `null` → `undefined`, since `QueryObserverBaseResult.error` is typed `TError | null` (TanStack's own convention) against `AsyncResource.error: ApiError | undefined`.
- **`injectMutation()` requires an active injection context, unlike the httpResource family's `createMutation()`.** The httpResource mutations are plain `HttpClient` calls with no such requirement; TanStack's are not. Since a component must call `create()`/`update()`/etc. the same way regardless of which implementation it received, `AsyncMutation`'s doc comment in `core/interfaces/async-resource.ts` now states the stricter (TanStack) requirement as the contract for both.
- **`retry: false` is set on the shared `QueryClient`** (`provide-tanstack-data-layer.ts`) for both queries and mutations. TanStack's default (3 retries, exponential backoff) would silently re-attempt failures this app treats as final (a 400 or 404 isn't transient), and the one genuinely retryable case — an expired access token — is already handled once, synchronously, inside `error.interceptor.ts`, before TanStack ever observes a failure.

**Testing TanStack-backed resources needed real investigation into `@tanstack/query-core`'s actual source** (shipped as readable TypeScript in `node_modules/@tanstack/query-core/src/`, not just `.d.ts`), because the first two testing approaches that worked for `httpResource()` did not transfer:

1. `ApplicationRef.whenStable()` alone resolved _before_ the query's state ever settled. Cause, confirmed by reading `create-base-query.mjs`: the Angular `PendingTasks` entry `whenStable()` waits for is only registered _inside_ the callback passed to `observer.subscribe(notifyManager.batchCalls(...))`, and `notifyManager`'s default scheduler (`@tanstack/query-core`'s `systemSetTimeoutZero`) dispatches that callback via a real `setTimeout(0)` — a macrotask, not a microtask. `whenStable()`, called immediately after a synchronous `HttpTestingController.flush()`, checks stability before that macrotask has ever fired, so nothing is pending yet and it resolves immediately.
2. The fix is **not** to await a real `setTimeout(0)` in every test (workable, but it litters every test with a timing detail and a small real delay). Instead, `notifyManager.setScheduler((callback) => callback())` in `beforeEach` (restored to the real default in `afterEach`) makes TanStack's own notification dispatch synchronous for the whole suite — a one-line, documented override rather than a per-test tactical wait.
3. Even with that override, one `await Promise.resolve()` per query assertion was _still_ not always sufficient — `Mutation#execute`/the query fetch path cross more than one microtask boundary internally (`createRetryer`, `firstValueFrom`), and the exact number isn't a contract worth hardcoding against. `await vi.waitFor(() => { TestBed.tick(); if (resource.isLoading()) throw new Error(...); })` — Vitest's poll-until-it-passes helper — is used for query assertions instead, so the test doesn't depend on TanStack's internal call-stack depth. Mutations only ever needed one `await Promise.resolve()` before the HTTP request appeared (`Mutation#execute` is a single `async` method, so exactly one hop), and the mutation's own returned `Promise` can just be awaited directly for the result, sidestepping the polling question entirely for that half of the test.

## Rendering strategy

| Route                                                     | Render mode | Why                                                     |
| --------------------------------------------------------- | ----------- | ------------------------------------------------------- |
| `/`, `/categories`                                        | Prerender   | Static, slow-changing content                           |
| `/restaurants`, `/restaurants/:id`, `/categories/:slug`   | Server      | Primary SEO surfaces; need per-request meta and JSON-LD |
| `/login`, `/register`, `/favorites`, `/profile`, `/my/**` | Client      | See below                                               |
| `/**`                                                     | Server      | 404 with correct HTTP status                            |

Every protected route is client-rendered by necessity, not convenience: the access token lives in memory only, and the refresh token is an `httpOnly` cookie scoped to `/api/auth/` — the Angular SSR server never receives it. Server-rendering a protected page would only ever produce a logged-out shell.

**`/` is not actually a defined Angular route yet** (`features/home/` is a later commit — see PLAN.md § Project structure) — the row above states the intended eventual render mode, not today's reality. Discovered while implementing commit 10: with `app.routes.ts` empty, the `**` → `Prerender` catch-all happened to still emit a static `index.html` for `/` per locale, an accident of there being no real routes for the build-time extractor to walk rather than evidence `/` was actually routed. Once `login`/`register` became real routes, the extractor started properly enumerating only genuine routes, `/` stopped being one of them, and the built SSR server now returns a bare `404 Cannot GET /en/` for it (verified with a real `curl` against `dist/restaurant-directory/server/server.mjs`). Not a regression introduced by commit 10's code — a pre-existing gap that commit 10's routing was the first to make visible. Resolves itself once the home-page commit adds a real `path: ''` route.

## i18n

Native `@angular/localize`, compiled once and inlined per locale (`I18nInliner`, part of `@angular/build:application`) — not a runtime translation library. `subPath` in `angular.json`'s `i18n` block sets both the URL prefix and the HTML base href, so the Angular route table itself is locale-agnostic: `routerLink="/restaurants"` resolves to `/es/restaurants` or `/en/restaurants` depending on which compiled bundle is currently loaded. `@angular/ssr`'s `AngularAppEngine` serves every locale from a single Node process, reading the locale from the first URL path segment per request.

Full detail: `prompt_restaurant_frontend.md` § Internationalization (not committed — internal reference only).

## UI theme, shell & dark mode

**Custom M3 palette — generated once, committed as a static file, not regenerated per build.** `mat.theme()` only accepts an already-computed M3 tonal palette map (verified against `@angular/material/core/theming/_definition.scss`: `primary: map.get($color-config, primary) or palettes.$violet-palette` — never a bare hex string). Computing one from a seed color requires Material Color Utilities, which has no pure-Sass equivalent, so `src/styles/_theme-colors.scss` was generated via the real, non-interactive schematic:

```
ng generate @angular/material:m3-theme --primary-color="#C2410C" \
  --tertiary-color="#0F766E" --directory=src/styles --defaults
```

(`--interactive=false` alone produced no output at all with no error — `--defaults` was also required to actually run non-interactively; the `--directory` flag also doesn't create a missing directory, it string-concatenates onto the filename if the directory doesn't exist yet, so the first attempt produced `src/styles_theme-colors.scss` rather than `src/styles/_theme-colors.scss` — `mkdir -p src/styles` first, then move the file, fixed it.)

**Light/dark schemes are three separate `mat.theme()` calls nested under different selectors**, not one call with a dark-mode flag (`src/styles/_theme.scss`): a base `html { }` (light, plus typography/density), a `@media (prefers-color-scheme: dark)` block, and explicit `[data-theme='light']`/`[data-theme='dark']` overrides (the last two win over the media query via attribute-selector specificity, regardless of source order). Verified by inspecting the actual compiled `styles.css`, not assumed from the Sass source: within each selector's emitted rule, `mat.theme()`'s own `--mat-sys-surface` value appears first, and this file's explicit cream/ink overrides (`#FDFBF7`/`#1C1917` — exact values from PLAN.md, close to but not identical to the M3 generator's own algorithmic neutral tones) appear after it in the same rule, so they win via ordinary "last declaration wins" cascade — confirmed present for all four scheme/override combinations in the built CSS.

**Fonts are self-hosted via `@use` inside `styles.scss`, not `angular.json`'s `styles` array.** `@angular/build:application`'s schema requires every `styles` array entry to be an actual file path ending in a recognized extension — a bare package specifier like `"@fontsource-variable/inter"` fails schema validation (`must match pattern "\.(?:css|scss|sass|less)$"`), even though this exact bare-specifier form is the documented usage in fontsource's own README (written for older, webpack-based tooling). `@use '@fontsource-variable/inter/index.css' as inter;` (aliased — both packages' entry points are named `index.css`, and Sass rejects two unaliased modules sharing a namespace) works instead.

**`ThemeService`** (`core/services/theme/theme.service.ts`) holds the explicit light/dark preference (`undefined` meaning "defer to the OS"), applies it via a `data-theme` attribute on `<html>` through an `effect()` (the only way to reach an element outside Angular's own component tree — `DOCUMENT` injection, not a template host binding), and persists it in a plain (non-`httpOnly`) cookie. Reading the initial value is SSR-request-scoped: the `REQUEST` injection token's cookie header on the server, `document.cookie` in the browser.

**Verified end-to-end against a real built-and-served response, with an important honest caveat about what that verification actually covers right now.** A `curl` with a `Cookie: theme=dark` header against the built SSR server correctly returned `<html data-theme="dark">`, and `theme=light`/no cookie behaved correctly too — but only after temporarily changing `app.routes.server.ts`'s catch-all from `RenderMode.Prerender` to `RenderMode.Server` for the test, then reverting. With today's actual config (`**` → `Prerender`, since `app.routes.ts` has no real client routes yet to be more specific about), **every route in the app is currently prerendered** — confirmed by the build log itself ("Prerendered 2 static routes" for `/es` and `/en`, "Prerendered 0 static routes" with the temporary `Server` override). A prerendered route has no live request at build time, so `ThemeService` always reads no explicit preference there, and a user's saved preference only gets applied after hydration corrects the `data-theme` attribute client-side — a single-frame flash, on every route, until real feature routes with `RenderMode.Server` exist. This is groundwork verified to work correctly, not a currently-active behavior end-to-end — it starts doing real work as soon as commits 11+ add server-rendered feature routes.

**`window.matchMedia` doesn't exist in jsdom at all**, discovered when `ThemeService`'s OS-preference fallback threw `matchMedia is not a function` in any spec that transitively constructed it (`MainToolbar` → `ThemeToggle` → `ThemeService`). Rather than stub it per-spec, `src/test-setup.ts` (wired via `@angular/build:unit-test`'s `setupFiles` option in `angular.json`, and added to `tsconfig.spec.json`'s `include` so it's actually type-checked) provides a default no-preference stub globally; specs that need to control the OS preference (`theme.service.spec.ts`) still override it locally per test.

**Auth-aware toolbar content (login/register links, later a `UserMenu`) is deferred to commit 10.** `MainToolbar` in this commit is wordmark + theme toggle + language switcher only — `AuthStore` doesn't exist yet, and `app.routes.ts` is still empty, so a `routerLink` to `/login` would point at a route that doesn't resolve to anything; a toolbar link that visibly does nothing on click is worse than no link at all.

**No `new Date()` in `SiteFooter`.** AGENTS.md's template rule against assuming globals like `new Date()` are available is partly an SSR-hydration-mismatch concern: a value computed once during SSR render and recomputed at client hydration is a mismatch risk in general, even though a copyright-year rollover is a vanishingly rare instance of it. The footer has no dynamic year as a result — a static tagline only.

## Option A visual restyle — design-system tokens

PLAN.md's Phase 3 applies a full visual design (`design-reference.html`, built from `design-brief.md` — both gitignored, local-only) to every screen. `src/styles/_tokens.scss` gained two additions ahead of any component work:

**Three more `--mat-sys-*` roles needed the same hand-correction `$surface-light`/`$ink-light` already got.** Checked against a real compiled build (`grep` on `dist/.../styles.css`, not assumed), the M3 generator's computed light-scheme values for `--mat-sys-primary` (`#ac3400`), `--mat-sys-on-surface-variant` (`#59413a`), and `--mat-sys-outline-variant` (`#e1bfb5`) all drift further from the approved palette (`#C2410C`/`#78716C`/`#E7E0D6`) than surface/ink did. `tokens.$primary-light`/`$muted-light`/`$border-light` + the new `light-role-overrides` mixin correct all three — **light mode only**, included in `_theme.scss`'s base `html {}` block and its `[data-theme='light']` block, deliberately _not_ in the `@media (prefers-color-scheme: dark)` or `[data-theme='dark']` blocks, so dark mode keeps exactly what the generator already produces for these roles, unexamined and unchanged. Verified in the compiled CSS: both light contexts resolve to the corrected hex, both dark contexts still resolve to the generator's own `#ffb59d`/`#ffdbd0`/`#59413a`.

**Three brand-new tokens with no `--mat-sys-*` equivalent at all**: `--app-ink-rgb` (`28 25 23`, space-separated — not comma — so consumers compose `rgb(var(--app-ink-rgb) / <alpha>)`, the modern slash-alpha syntax; a literal 4-arg `rgba(var(--x), a)` doesn't work since the substituted custom property fills one argument slot, not three), `--app-card-shadow`, and `--app-chip-teal-bg`/`--app-chip-teal-fg`. These are light-mode-tuned only by design (their rgba base is literally the light scheme's ink color) and applied unconditionally via `tokens.app-tokens`, not swapped per scheme — a documented, narrow dark-mode gap rather than a new dark theme, consistent with `design-brief.md`'s own scoping of Option A to light mode.

A Sass gotcha hit while adding `--app-card-shadow`: `rgba($ink-light-rgb, 0.05)` where `$ink-light-rgb` is a 3-item list fails with `$color: (28 25 23) is not a color` — Dart Sass's own `rgba()` function tries to evaluate the list as its 2-arg `rgba(color, alpha)` overload rather than passing it through as CSS. Same failure with a bare `rgb(#{$ink-light-rgb} / 5%)` interpolated into the function call — Sass still parses `rgb()` specially and rejects the space-list as an invalid single red-channel value. Fixed by building the whole value as an opaque Sass string first (`#{'rgb(#{$ink-light-rgb} / 5%)'}`) so Sass never re-parses it as its own color function, only re-emits it as CSS.

**A fourth, visually severe instance of the same drift — only caught by actually rendering `RestaurantCard` in a browser, not by reading the Sass source.** A real screenshot (mocked Playwright run against `/en/restaurants`, not a live backend) showed every card and image placeholder rendering saturated pink instead of the reference's near-white/cream. Root cause, confirmed in the compiled CSS: the M3 generator's light-scheme "container" surfaces — `--mat-sys-surface-container-low` (`#fff1ed`), `-high` (`#fce3db`) — are the role `RestaurantCard`'s background and `.cover.placeholder` both read, and both are far more saturated than the approved `#FFFDFA` card / pale-tan placeholder. `light-role-overrides` gained `$surface-container-light`/`$surface-container-high-light`, applied to `--mat-sys-surface-container`, `-container-low`, `-container-high`, and the older M2-era `--mat-sys-surface-variant` alias (`ImageUploader`'s empty-state background reads that one specifically) — light mode only, same pattern as the primary/on-surface-variant/outline-variant fix above. Re-screenshotted after the fix to confirm visually, not just by re-reading the compiled hex.

### Shared presentational components (commit 23)

All 13 of `src/app/shared/components/` restyled to the reference — no template restructuring beyond what a pure style pass needs, and no logic changes:

- **`RestaurantCard`** — the highest-impact single change (reused on `/`, `/restaurants`, `/favorites`): added a bottom-anchored dark gradient overlay and a blurred "★ rating" pill (both via `--app-ink-rgb`) on the cover image, added `box-shadow: var(--app-card-shadow)`, moved `CategoryChips` above the rating/price row, added a `border-top` divider before that row, and right-aligned the price badge. The rating badge's number comes from `DecimalPipe` (`| number: '1.1-1'`) — a new template pipe, not new logic, and marked `aria-hidden` since `RatingStars` already provides the accessible rating text.
- **`SkeletonCard`** — matched to `RestaurantCard`'s real background (`surface-container-low`, was `surface-container` — an existing inconsistency, fixed in passing) and shadow, so the loading state doesn't visibly jump once real content arrives.
- **`CategoryChips`** — retinted from neutral `surface-container`/`on-surface-variant` to the new `--app-chip-teal-bg`/`-fg` tokens.
- **`PriceRangeBadge`** — retinted from muted gray to `--mat-sys-primary`, bolded — this is the one element shown in terracotta on nearly every reference screen (cards, dashboard rows, menu items).
- **`FavoriteButton`** — the favorited state now fills the circle solid `--mat-sys-primary` with a white heart, matching the reference's `/favorites`-page treatment, instead of keeping a white circle with just the glyph recolored.
- **`ImageUploader`** — fixed the one real inconsistency `design-brief.md` flagged (hardcoded `4px` radius instead of a `--mat-sys-corner-*` token, now `-corner-small`), retinted the upload button to a terracotta-outline pill, and added a new `shape` input (`'rect' | 'circle'`, default `'rect'`) so `ProfileForm`'s avatar (commit 29) can request a circular preview without every other caller's cover-photo thumbnail changing shape — a presentational prop only, no change to the file-picking/upload logic.
- **`AuthCard`**, **`PaginatorBar`** — shadow/corner-radius and muted-text-color pass respectively; no structural change.
- **`RatingStars`**, **`EmptyState`**, **`ErrorState`**, **`OpeningHoursTable`**, **`ConfirmDialog`**, **`CursorLoadMore`** — no changes needed. All five already read semantic `--mat-sys-*` roles (`primary`, `outline-variant`, `on-surface-variant`, `error`) that now resolve to the corrected palette automatically, or (Material's own dialog/button chrome) are already theme-driven with nothing this app hand-styles.

Verified with a real mocked-Playwright screenshot of `/en/restaurants` (not just `npm test`/`ng build` passing) both before and after the `surface-container` fix above — the described "verify UI changes in a browser" step for this commit, done via a throwaway spec file (not committed) rather than a manually-run dev server, since the real backend wasn't running locally at the time.

### App shell (commit 24)

`MainToolbar`, `SiteFooter`, `ThemeToggle`, `LanguageSwitcher`, `UserMenu` restyled per the reference's toolbar/footer spec (68px-equivalent `min-height: 4.25rem` toolbar, `1px` `outline-variant` border under it and above the footer, bold 22px Fraunces wordmark).

**A real, visible bug caught by the commit-23 screenshot, fixed here**: `MainToolbar`'s plain `<a routerLink>` elements ("Restaurants", "Log in", "Register") had zero styles of their own — no color, no `text-decoration: none` — so they rendered as default-blue underlined browser links, clashing with the rest of the theme. Only Material-directive elements (`mat-button`, `mat-menu-item`) were ever theme-colored automatically; a plain `<a>` isn't. Fixed with a scoped `a:not(.wordmark)` rule; `routerLinkActive="active"` added to the "Restaurants" link for the reference's active-tab underline treatment (a template addition, not new logic — `RouterLinkActive` computes the match itself).

**`UserMenu` gained a real avatar** — a real `user().avatar` image when the user has uploaded one (`ProfileForm`, commit 8), or a solid-teal circle with computed initials otherwise, mirroring the `@if (image) {...} @else {...}` placeholder pattern already used everywhere else in the app (`RestaurantCard`, `ImageUploader`). Uses `--app-chip-teal-fg` (already the exact reference teal, `#0F766E`) rather than hunting down a fifth `--mat-sys-tertiary` drift — `--mat-sys-tertiary` compiles to `#006a63` in light mode, a different, unrelated teal, and this is the only place in the app that needs a _solid_ teal fill rather than a tint/foreground pairing, so reusing the existing chip token was simpler than adding a new one. One real layout bug hit while building this: putting the flex layout class directly on `<button mat-button>` made the avatar and username stack vertically instead of sitting inline — Material's own internal button-label wrapper fights a `display: flex` applied to the host element. Fixed by moving the layout class to a plain `<span>` wrapping the button's content instead of the button itself.

## Models

Each resource has one `*.model.ts` file in `core/models/` holding three things together: the raw `*Dto` interface (snake_case, matching the API's wire format exactly — decimals and dates as strings), the app-facing model (camelCase, decimals parsed to `number`, timestamps parsed to `Date`), and a `to*()` mapper function between them. Resources whose write shape differs from their read shape (`RestaurantWrite` vs. `Restaurant`, `ReviewCreate`/`ReviewUpdate` vs. `Review`) get separate write types rather than one interface with optional fields — see `docs/API.md` for which resources this applies to.

`core/utils/decimal.ts` (`parseDecimal`) and `core/utils/date.ts` (`parseApiDate`) back every mapper and throw on invalid input rather than silently producing `NaN`/`Invalid Date`. `core/utils/error-mapper.ts` (`mapApiError`) normalizes every DRF error shape into one discriminated `ApiError` union, branching on response shape and status code, never on message text. `core/utils/api-url.builder.ts` handles query serialization for the page-number and limit/offset pagination styles (structurally identical, both `{ count, next, previous, results }`); cursor pagination is deliberately unsupported there — a `cursor` value must only ever come from a `next`/`previous` URL the API already returned.

## Environment configuration

`environment.ts` (dev) hardcodes `apiBaseUrl: 'http://localhost:8000/api'` — a conventional local default, not a deployment secret. `environment.prod.ts` reads a bare global identifier, `NG_APP_API_BASE_URL` (declared in `environments/ng-app-globals.d.ts`), swapped in via `fileReplacements` on the `production` build configuration.

That identifier is not `process.env.API_BASE_URL`: `@types/node`'s ambient `process` types every env value as `string | undefined`, which would force a non-null assertion on every read despite the build guaranteeing a literal string. Since `angular.json` is static JSON and can't embed a shell-interpolated value, `scripts/build.mjs` wraps `ng build` — it loads `.env` via `process.loadEnvFile()` when present (a no-op on Render, which injects env vars into the process directly), fails fast if `API_BASE_URL` is unset, and passes `--define=NG_APP_API_BASE_URL=<value>` so esbuild replaces the identifier with a literal string at build time, in both the browser and server bundles. `npm run build` runs this wrapper; `npm run watch` (the `development` configuration) never references the identifier at all, so it needs no wrapper.

## Auth

`AccessTokenStore` (`core/services/auth/`) holds the access token as a signal, never persisted — nothing else. It's deliberately split from the full `AuthStore` (user profile, login/logout orchestration, `isOwner`/`isAuthenticated`, added later): the interceptors only ever need the token itself, and this keeps them independent of the larger store.

`auth.interceptor.ts` attaches the bearer token and `withCredentials: true` — but only to requests targeting `environment.apiBaseUrl`. This scoping is load-bearing, not cosmetic: an unscoped interceptor would leak the JWT to third-party requests such as the direct-to-Cloudinary upload.

`error.interceptor.ts` implements 401-retry-once via `auth/refresh/`, excluding `auth/login/`, `auth/register/`, and `auth/refresh/` itself from the retry (see `docs/API.md`), and is scoped to our own API the same way. The actual refresh call is coordinated by `TokenRefreshCoordinator`, which deduplicates concurrent 401s into a single in-flight `auth/refresh/` call — several requests failing near-simultaneously as the token expires is a mainline scenario, and without coordination each would trigger its own refresh, racing against the backend's refresh-token rotation. It calls the API via a raw `HttpClient` built on `HttpBackend`, bypassing the interceptor chain (the normal `HttpClient` would re-enter `error.interceptor.ts` itself). It's `@Service()`-scoped rather than a module-level variable deliberately, since Angular SSR's per-request injector means module-level state would otherwise leak between concurrent users' requests in the same Node process.

`NotificationService` (`core/services/notification/`) is a thin `MatSnackBar` wrapper `error.interceptor.ts` uses to toast on 429s; message text is authored with `$localize` directly in the `.ts` file (`error.interceptor.ts`'s throttled-message string is the first translated string in the app, verified end-to-end against a real localized build).

**`AuthStore`** (`core/services/auth/auth.store.ts`, commit 10) composes `AccessTokenStore` rather than duplicating it, adding the user profile (`signal<UserProfile | null>`), `isAuthenticated`/`isOwner` computeds, and login/register/logout orchestration via `AuthApi` (`core/services/auth/auth.api.ts` — a thin `HttpClient` wrapper for `auth/register/`, `auth/login/`, `auth/logout/`, `users/me/`; `auth/refresh/` stays exclusively `TokenRefreshCoordinator`'s job).

**Session rehydration on reload was not specified anywhere in the original plan — decided during implementation, at the user's direction.** The access token is memory-only, so without extra work a page reload always starts logged out even with a still-valid `httpOnly` refresh cookie. On the browser, `AuthStore`'s constructor makes one silent attempt — `TokenRefreshCoordinator.refresh()` (reused, not duplicated) then `AuthApi.me()` — before setting an `initialized` signal, either way. On the server, `initialized` is set immediately, since there is no browser cookie jar to rehydrate from server-side (protected routes are client-rendered only, per the table above).

All three guards (`auth.guard.ts`, `owner.guard.ts`, `guest.guard.ts`) — functional `CanActivateFn`s, no class-based guard precedent exists in this codebase — wait for `AuthStore.initialized` (`toObservable(initialized).pipe(filter(Boolean), take(1))`) before deciding, so a reload doesn't redirect to `/login` before the rehydration attempt has resolved. `MainToolbar` gates its auth-aware content (login/register links vs. `UserMenu`) on the same `initialized()` signal, for the same reason — SSR/prerendered output always renders the logged-out branch, the same class of accepted single-frame post-hydration flash already documented for dark mode above.

A standing `effect()` inside `AuthStore` clears the user profile whenever `AccessTokenStore`'s token goes back to `null` — this is what makes `error.interceptor.ts`'s existing forced-logout-on-failed-refresh (`tokenStore.set(null)`) also clear the profile, with zero direct coupling between the interceptor and this store.

`role` on `UserProfile` is treated as UX convenience only, never a security boundary, since the backend enforces the same rule independently — `owner.guard.ts` exists to avoid rendering a dashboard shell the API would reject, not as the actual security boundary.

## Restaurant listing

`/restaurants` (`RestaurantListPage`) drives its filters and pagination entirely from the URL's query params — this is what makes `RenderMode.Server` on this route actually meaningful (each filter combination its own crawlable, server-rendered URL) rather than a client-only view of shared state. Wired via `withComponentInputBinding()`, added to `provideRouter()` in `app.config.ts` (first use in this project): verified against the real `@angular/router` source (`RoutedComponentInputBinder` in `_router-chunk.mjs`) before relying on it — it merges `queryParams`/`params`/`data` by key and calls `setInput()` on every matching declared `input()` of the routed component, including `undefined` when a param is absent (default `unmatchedInputBehavior: 'alwaysUndefined'`). Query-param values always arrive as strings; `page`/`minRating` use an `input(..., { transform })` with `numberAttribute` (coercing an absent/invalid value to a fallback, not `NaN`).

**Verified end-to-end against the live backend, not just a mocked test.** A `curl` against the built-and-served app returns `/en/restaurants` with the real restaurant list already in the server-rendered HTML; `?city=Guadalajara` correctly narrows the server-rendered result to the one matching restaurant; a filter matching nothing correctly server-renders `EmptyState`'s message. `RestaurantFilters` applies on an explicit "Apply" submit, not live-per-keystroke — a live-apply UX would trigger a new SSR navigation on every keystroke, defeating the reason this route is server-rendered rather than client-rendered.

**`PaginatorBar` is a hand-rolled prev/next control, not `mat-paginator` — reversed mid-implementation after a real build measured the cost.** First built as a thin wrapper translating `mat-paginator`'s 0-indexed `pageIndex` to this app's 1-indexed `page`. A real `npm run build` showed `MatPaginatorModule` alone added ~20kB to the initial bundle — `@angular/cdk`'s overlay/a11y/bidi machinery (the first would-be use of CDK Overlay in this project), almost entirely in service of a page-size picker this app doesn't expose (page size is fixed per resource — see `docs/API.md` — not a user control, so `PaginatorBar` isn't one either). Rewritten as two `mat-icon-button`s (`MatButtonModule`, already loaded elsewhere for `AuthCard`/`ErrorState`) plus a "Page X of Y" indicator; the `restaurant-list-page` lazy chunk dropped from 89.5kB to 59.75kB as a result. The remaining real weight — `RestaurantFilters`' `MatSelect`/`MatFormField`/`MatInput`/`ReactiveFormsModule`, the app's first real filter form — is unavoidable; `angular.json`'s bundle budget was raised `700kB` → `725kB` for that specifically, with the CDK-overlay cost deliberately engineered away rather than budgeted around.

`PriceRangeBadge` (not `PriceRange`, PLAN.md §2's original name) — a component named `PriceRange` would collide with `core/models/restaurant.model.ts`'s existing `PriceRange` type in any file needing both, e.g. `RestaurantCard`.

`RestaurantCard` doesn't yet link to `/restaurants/:id` — that route lands in commit 12; same reasoning as `MainToolbar` deferring auth links in commit 9 until `/login`/`/register` existed.

## Restaurant detail

`/restaurants/:id` (`RestaurantDetailPage`) is display-only in this commit — `RestaurantHero`, `MenuSection`, `ReviewsSection`. Creating/editing/deleting a review (`ReviewForm`) is commit 13's job, per PLAN.md's commit plan.

**Verified end-to-end against the live backend.** A `curl` against the built-and-served app returns the restaurant's name/address/city already server-rendered for a real id; a nonexistent id correctly server-renders `ErrorState` with the API's actual `"No Restaurant matches the given query."` message (via `mapApiError`/`apiErrorMessage`), not a crash or a generic fallback. The outer HTTP status stays `200` for that case — only `/**` is required to carry a real `404` per the rendering-strategy table above; an in-page not-found inside an otherwise-`200` SSR shell is ordinary SPA behavior, unchanged here.

`ReviewDataService.loadMore(url)` (defined in commit 7/8, exercised for the first time here): `RestaurantDetailPage` seeds an accumulator signal from the first cursor page (via an `effect()` watching the base resource, same pattern as `AuthStore`'s rehydration sync), appends each `loadMore()` result, and always uses the API's own opaque `next` URL verbatim — the standing "never construct a cursor" rule from `docs/API.md`.

`MenuSection.groupMenuItems()` is exported as a plain function, not template logic, specifically so it's unit-testable directly — groups a flat menu-item list into a fixed category order (appetizer → main course → beverage → dessert → other), dropping empty categories.

`ErrorState.message` widened from `input<string>(defaultText)` to `input<string | undefined>()`, with the fallback now applied in the template (`message() ?? defaultMessage`) instead of as the input's default value — needed so `MenuSection`/`ReviewsSection`/`RestaurantDetailPage` can pass `apiErrorMessage()`'s `string | undefined` result straight through instead of each duplicating a ternary. Strictly widens the type; commit 11's `RestaurantGrid` usage (which always passes a definite string) is unaffected.

## Review write flow

A user has at most one review per restaurant (server-enforced — see `docs/API.md`), so create and edit are the same UI slot, never two separate flows. `RestaurantDetailPage` derives `myReview` by matching `AuthStore.user()?.id` against the loaded reviews and owns the `create`/`update`/`remove` mutations plus a single `isReviewFormOpen` toggle; `ReviewsSection` (still presentational) derives `otherReviews` by excluding `myReview`, so it's never rendered twice. `ReviewForm` is presentational and mode-agnostic — an input `review: Review | undefined` (present = edit, absent = create) is all it needs; the parent decides which mutation to call, same pattern as `LoginPage`/`RegisterPage` (commit 10).

`ReviewForm`'s output originally named `cancel` had to be renamed to `cancelled` — `@angular-eslint/no-output-native` correctly caught it colliding with the native DOM `cancel` event.

**`ConfirmDialog` is the first `MatDialog` use in this project**, gating review deletion. Measured with `npm run build -- --stats-json` + `jq` against the resulting `stats.json` (the workflow this project settled on this session, replacing the manual-isolation approach from commit 11): total growth was only ~8kB, dominated by CDK's `_a11y-module-chunk.mjs` (~12kB — focus-trap/live-announcer, genuinely required for an accessible modal) offset by minor shifts elsewhere. `_overlay-module-chunk.mjs` itself barely moved, since `MatMenu` (commit 10) already pays that cost. Bundle budget raised `740kB` → `745kB`; nothing wasteful was found to cut this time, unlike `MatPaginator` in commit 11.

Verified against the live backend for what doesn't need authenticated credentials (none available in this environment): `curl` confirms `/restaurants/:id` still server-renders correctly and the new "Log in to write a review" prompt appears for an anonymous visitor. The authenticated write flow itself (create-vs-update branching, confirm-then-delete sequencing, mutation payload shapes) is covered by `restaurant-detail-page.spec.ts` against faked `ReviewDataService`/`AuthStore`/`MatDialog` — an accepted gap in live verification, not a skipped step.

## Favorites

`FavoritesStore` (`core/services/favorites/favorites-store.ts`) holds a single `signal<Set<string>>` of favorited restaurant ids, shared app-wide so the heart icon on `/restaurants`, `/restaurants/:id` and `/favorites` all agree. `toggle(restaurantId)` is optimistic: flips the id immediately, reconciles with the server's authoritative `{ favorited }` response from `POST favorites/toggle/` on success, rolls back on failure.

**The seed fetch must never fire for an anonymous visitor** — `GET favorites/` requires auth, and firing it anyway would trip `error.interceptor.ts`'s 401-retry-then-forced-logout path for someone who was just browsing a public page. The first implementation gated this by nesting a second `effect()` inside the store's constructor effect, recreating the seed resource whenever `AuthStore.isAuthenticated()` changed — this throws `NG0602` ("effect() cannot be called from within a reactive context") at runtime, not just under test; effects cannot be created from inside another effect's callback, full stop, regardless of which injector is passed. Fixed by widening `FavoriteDataService.list()` from `list(query: Signal<FavoriteQuery>)` to `list(query: Signal<FavoriteQuery | undefined>)` — `undefined` skips the fetch entirely, the same convention `RestaurantDataService.byId(id: Signal<string | undefined>)` already established — across the interface and both `http-resource`/`tanstack` implementations (the `http-resource` variant returns `undefined` from the URL callback to skip; the `tanstack` variant sets `enabled: q !== undefined`, matching `byId`'s own `enabled: id() !== undefined` there). The resource is now created once, directly in the constructor (an ordinary injection context, no nesting); a single top-level `effect()` reacts to both `AuthStore.isAuthenticated()` and the resource's own value.

`FavoriteButton`'s `toggle` output had to be renamed to `toggled` — `@angular-eslint/no-output-native` flagged the same class of collision `ReviewForm`'s `cancel` → `cancelled` did.

`FavoritesPage` reuses `RestaurantGrid` rather than a new list component — needed one small, backward-compatible widening: `RestaurantGrid.emptyMessage` became an optional override `input<string>()` (falling back to the existing generic "No restaurants match your filters." text) so this page can show its own "You haven't added any favorites yet." instead.

## E2E testing (mocked suite)

`playwright.config.ts` + `e2e/` (`fixtures/`, `mocks/`, `specs/`) — a mocked Playwright suite covering the flows commits 10–14 shipped, backfilled as its own commit rather than landing per-commit as AGENTS.md's workflow rules call for (an unrecorded process gap, distinct from the `ng serve` gap below, which is a deliberate, documented deferral).

**Runs against a real production build + `serve:ssr:restaurant-directory`, not `ng serve`.** Verified empirically while wiring this up, contradicting the initial assumption: `ng serve` runs the exact same `server.ts` SSR middleware in dev too — not a plain static dev server — so it hits the `/` → `/es/` redirect gap (see "Rendering strategy"'s sibling note in PLAN.md's "Known follow-up" section) on **every** route, not only locale-prefixed ones. `webServer.command` is `npm run build && npm run serve:ssr:restaurant-directory`; specs navigate under the `/en/...` prefix.

**Fixtures are recorded from a real local backend**, `page.route()`-mocked in tests (never touched at runtime, no backend required to run the suite). Authenticated/mutating fixtures (login, favorites toggle, review create/update/delete, an owner-role user for the owner-CRUD suite) come from real credentials used transiently to record and immediately clean up (toggled a favorite back off, deleted a throwaway review) — every access token is replaced with a placeholder string before committing, never a real JWT. A few fixtures (`429`, a successful registration, a synthetic second reviews page — the real seeded data only has one review total) are hand-authored against `docs/API.md` instead, where provoking them for real wasn't worth the side effects. `e2e/mocks/api-mocks.ts` matches routes **by pathname only, never by query string** — Playwright's glob syntax treats `?` as a single-character wildcard, not a literal query separator, so pathname predicates are the unambiguous way to target one REST resource regardless of which query params a given request carries; the two endpoints hit with genuinely different, concurrently in-flight query shapes (`/favorites/`'s app-wide seed vs. a page's own paginated list; `/reviews/`'s first page vs. a cursor "load more") branch internally on the query string instead.

**A real rendering bug was found and fixed, caught directly by this suite's error-state test, not by code review.** `RestaurantListPage`, `RestaurantDetailPage` and `FavoritesPage` each read a resource's `.value()` directly in the template right alongside a separate `.error()` binding — e.g. `[restaurants]="restaurants.value()?.results ?? []"` next to `[error]="restaurants.error()"`. `resource.value()` re-throws the underlying error once a resource has failed (Angular's own documented `WritableResource` behavior), so any API error threw mid-render and silently blanked the whole page — no skeleton, no error message, nothing — before `RestaurantGrid`'s `ErrorState` branch ever got a chance to run. Confirmed via a diagnostic Playwright test capturing `console`/`pageerror` events, not guessed. Fixed everywhere by guarding on `.error()` first: `restaurantsPage = computed(() => resource.error() ? undefined : resource.value())`, then binding to that computed instead of the raw resource in the template. This is now the standing pattern for any new smart page (applied from construction in the owner-CRUD commit, not retrofitted).

**Testing philosophy was corrected afterward, across two follow-up commits** (`cab0d4d`, `5576f3b`): unit specs are for logic (a computed value, a validation rule, a payload-shape decision), not view/interaction behavior, which this E2E suite (or manual testing) proves just as reliably. Specs — including some predating this suite, from commits 10 and 13 — were trimmed once they duplicated a scenario E2E now proves with equal precision (exact URL, exact error text, not just "something rendered"). Shared, multi-consumer components (`PaginatorBar`, `RatingStars`, `RestaurantGrid`, etc.) are the one exception, keeping their own tests regardless of any one consumer's E2E coverage. Net: -35 tests, -1 file, one real test-ordering bug fixed along the way (`register-page.spec.ts` was silently relying on a stale mock left over from a test removed in the same trimming pass — the same class of bug `favorites-store.spec.ts` had earlier in the same session). Full rule in AGENTS.md §6.

Not built: the `@live` smoke suite (a later, separate commit) and locale-prefix/language-switcher coverage (`/` has no real route yet — no home-page feature — and `ng serve`'s gap makes this moot regardless).

## Owner dashboard and restaurant CRUD

`/my/restaurants` (`MyRestaurantsPage`), `/my/restaurants/new` and `/my/restaurants/:id/edit` (`RestaurantFormPage` → `RestaurantForm`) — all `RenderMode.Client` (same reasoning as `/favorites`: owner-only, user-specific, no SEO value) and gated by `owner.guard.ts`, unused since it was built alongside the other guards. Exercises `RESTAURANT_DATA.mine()/create()/update()/remove()` and `CATEGORY_DATA`, both unused in application code since the data-layer commits.

**`cover_image` and `opening_hours` are deliberately not form fields — a scope decision with no data-loss risk, not an oversight.** `cover_image` needs Cloudinary (a later commit — see "Menu item management and Cloudinary uploads" below, where this changes: once the form can round-trip the real value, `cover_image` moves off the omitted-on-update list entirely); `opening_hours` needs a real weekday-schedule editor, a distinct UI unit with no established pattern yet (`OpeningHoursTable` is read-only display only). `create()` sends the full `RestaurantWrite`, so a new restaurant gets valid empty defaults (`cover_image: ''`, `opening_hours: {}`, both acceptable per the API); `update()` takes `Partial<RestaurantWrite>`, so the edit form's submitted body omits both keys (and `latitude`/`longitude`, per the project's standing "no embedded map" decision) entirely — the backend leaves a restaurant's existing cover image or hours untouched on PATCH, rather than a blank form silently wiping them. `RestaurantForm` is the first non-auth form using `applyFieldErrors` (`core/utils/apply-field-errors.ts`); its control names stay snake_case to match `RestaurantWrite`'s DTO keys exactly, the same reason `LoginPage`/`RegisterPage` do this.

`MyRestaurantsPage`/`RestaurantFormPage` guard every resource's `.value()` behind `.error()` from construction — the pattern the E2E suite's bug fix (above) established, applied here from the start rather than retrofitted.

**A bundle-report artifact was found and correctly diagnosed, not misread as a real regression, while checking this commit's budget impact.** After adding the three new lazy routes, `npm run build`'s summary table showed the initial bundle apparently dropping from ~720kB to ~180kB. Confirmed via `stats.json` (`jq` against `main-*.js`'s own `imports` array, per CLAUDE.md's `--stats-json` guidance) that this is not real: the ~558kB chunk the summary re-bucketed under "Lazy chunk files" is still reached via a plain `import-statement` from `main.js`, not a `dynamic-import` — genuinely eager, just extracted into its own cacheable file now that several lazy routes also reference it. Real eager weight (~717kB) is essentially unchanged from baseline; no budget warning fired, `angular.json` untouched. Takeaway for future bundle checks: the build's own "Initial total" row can undercount once a shared chunk becomes _also_ reachable via a dynamic import elsewhere — check `stats.json`'s per-chunk import **kind** (`import-statement` vs. `dynamic-import`) directly rather than trust the summary table's bucketing.

No unit specs for `MyRestaurantsPage`/`RestaurantFormPage`, per the corrected testing philosophy above — every piece of their behavior is either an already-proven pattern elsewhere (`RestaurantListPage`'s error/loading/empty branching, `RestaurantDetailPage`'s `ConfirmDialog` delete flow) or exercised by `e2e/specs/owner-crud.spec.ts`, which asserts the create-vs-edit request-body asymmetry directly against the real captured POST/PATCH body (`route.request().postDataJSON()`) rather than a mocked method call — more authoritative, since it proves the actual HTTP payload rather than a JS function call. Owner-role E2E fixtures are hand-authored against `docs/API.md` (no owner-role credentials were available to record real ones, the same fallback already used for `register-response.json`).

## Menu item management and Cloudinary uploads

`/my/restaurants/:id/menu` (`MenuManagerPage`) exercises `MENU_ITEM_DATA.create()/update()/remove()`, unused since commits 7/8. An inline create/edit form on one page, not a separate route — mirrors `RestaurantDetailPage`'s `ReviewForm` pattern exactly: `editingItem` presence decides create-vs-edit (plays `myReview`'s role), `isFormOpen` gates visibility separately since create mode also needs the form open with no item selected. `MenuItemTable` stays its own presentational component (grouping is meaningfully more complex than `MyRestaurantsPage`'s flat rows), reusing `groupMenuItems()` (`menu-section.ts`, already exported and tested) rather than reimplementing category grouping.

**`categoryLabel()` moved out of `menu-section.ts` into `core/utils/menu-item-category-label.ts`**, shared by `MenuSection` (public display), `MenuItemForm` and `MenuItemTable` (owner management) — avoided a third copy of the same switch statement after almost writing one directly in `MenuItemForm`. Kept the original `menuSection.*` i18n ids (the function only moved, the strings didn't change) specifically so the already-translated units in `messages.es.xlf` from commit 12 weren't orphaned by the move.

### Cloudinary uploads

`CloudinaryUploadService` (`core/services/upload/cloudinary-upload.service.ts`) is a **single-implementation `@Service()` utility, not a token+dual-implementation resource** — this codebase has two established patterns side by side (the `InjectionToken` + `http-resource`/`tanstack` pair for swappable API resources like `RESTAURANT_DATA`, and plain `@Service()` singletons for non-swappable infra like `NotificationService`/`ThemeService`/`TokenRefreshCoordinator`); Cloudinary has exactly one backend, so it follows the second pattern. `upload(file, folder)` makes two sequential calls through the **normally-injected** `HttpClient` — no `HttpBackend` bypass like `TokenRefreshCoordinator`'s (that one exists to dodge interceptor _re-entrancy_ on a 401 refresh, a different problem entirely): `POST {apiBaseUrl}/uploads/signature/` matches `environment.apiBaseUrl`, so `auth.interceptor.ts` attaches the bearer token correctly; the following `POST https://api.cloudinary.com/v1_1/{cloud_name}/image/upload` never matches that prefix, so the same interceptor's existing scoping guard excludes it by construction — confirmed by reading the guard directly (`auth.interceptor.ts`'s doc comment already named this exact call before this commit existed to write it).

**`ImageUploader` (shared, presentational) never performs HTTP** — matches every other form/data split in this codebase (`ReviewForm`, `RestaurantFilters`). It only picks a file (native `<input type="file" accept="image/*">`, wrapped in a `<label>` rather than a `viewChild` + programmatic `.click()` — one less thing to wire) and emits it; it renders whatever `imageUrl` input currently holds, the same `@if (imageUrl()) { <img [ngSrc]> } @else { placeholder div }` pattern `RestaurantCard`/`RestaurantHero` already use for `coverImage`, plus an `isUploading` spinner overlay (`MatProgressSpinnerModule`, already loaded elsewhere — no new bundle weight). The actual upload, and the round-trip of the resulting URL back down, is the smart parent's job: `ImageUploader` emits a `File` up to `RestaurantForm`/`MenuItemForm`, which re-bubble it to `RestaurantFormPage`/`MenuManagerPage`; the parent calls `CloudinaryUploadService.upload()`, tracks `isUploadingImage`, and feeds the resulting URL back down via a plain `input()` separate from the `restaurant`/`menuItem` input (which only represents the _loaded_ entity). `cover_image`/`image` are **not** Reactive Form controls in either form — nothing to type into — tracked as a signal the parent controls and merged into the emitted value manually at submit (`{ ...form.getRawValue(), cover_image: coverImageUrl() }`); submit is disabled while an upload is in flight so it can't race one.

**This simplified, not just extended, commit 15's create-vs-edit body asymmetry.** `cover_image` was omitted from `RestaurantFormPage`'s `update()` `Partial` body specifically because the form had no way to _preserve_ the existing value — sending it risked wiping it with an empty default. Now that the form faithfully round-trips the current value (defaults to the loaded restaurant's existing image when nothing new is uploaded), `cover_image` is safe to include in both create and update bodies unconditionally, same as every other field. Only `opening_hours`/`latitude`/`longitude` remain the omitted-on-update keys — still no editor for those, still no map. The exact same reasoning now applies to `MenuManagerPage` and `MenuItemWrite.image`.

**A real cross-file test-isolation bug surfaced writing `CloudinaryUploadService.spec.ts`, not a defect in the service.** `upload()` makes two sequential `await firstValueFrom(...)` calls — unlike a pure RxJS operator chain (`error.interceptor.ts`'s synchronous 401-retry-and-replay test needs no such wait), each `await` is a genuine microtask hop, so asserting on the second `HttpTestingController` request immediately after flushing the first found nothing yet. Fixed with `vi.waitFor()` (poll until the request appears) rather than guessing a fixed number of `Promise.resolve()` hops — same reasoning already used for this project's TanStack query assertions. Left unfixed, the failing test's `afterEach` (`httpMock.verify()`) threw and left `TestBed` in a dirty state for whatever ran next in the same Vitest worker — `auth.api.spec.ts`, an entirely unrelated, unmodified file, failed with a misleading `"Cannot configure the test module..."` error purely as collateral damage. Always re-run the full suite after adding a spec, not just the new file in isolation, to catch this class of bleed-through.

No unit specs for `MenuManagerPage`/`MenuItemTable`/`ImageUploader` — same reasoning as commit 15's `MyRestaurantsPage`/`RestaurantFormPage`: proven patterns, real coverage from `e2e/specs/menu-management.spec.ts` (list/create-with-upload/edit/delete) plus the extended `owner-crud.spec.ts` (the restaurant cover-image case, asserting the uploaded URL lands in the captured POST body). `MenuItemForm.spec.ts` exists for the same reason `RestaurantForm.spec.ts` does — real internal logic (patching, validation, payload shape, field errors) the E2E suite can't equivalently check, since it only ever sees the final network request.

Bundle budget checked via `stats.json`, not assumed: real eager weight ~718kB (`main.js` + the one shared eagerly-imported chunk, confirmed by import **kind** rather than the summary table's headline number — see the finding above), under the `745kB` warning threshold. `angular.json` untouched.

## Profile

`/profile` (`ProfilePage` → `ProfileForm`), `authGuard` (any authenticated user, not `ownerGuard` — every account has a profile) and `RenderMode.Client`, matching `/favorites`'s reasoning exactly. `UserMenu` already linked here since the favorites commit, with nothing behind it until now.

**No new `InjectionToken`/dual-implementation resource — `AuthStore` grows one more orchestration method instead.** There's exactly one current user, and `AuthStore` is already its single source of truth (per its own doc comment); `updateProfile()` follows the same shape as its existing `login`/`register`: `await firstValueFrom(authApi.updateMe(body))`, then set `userSignal` directly from the response. `AuthApi.updateMe()` is one more thin `PATCH users/me/`, mapped through the existing `toUserProfile`, same as `me()`.

**Reuses the existing shared `ImageUploader` for the avatar rather than a dedicated `AvatarUploader` component.** `ImageUploader` (built in the previous commit) already has everything an avatar picker needs — `imageUrl`/`isUploading` inputs, a `fileSelected` output, no HTTP of its own — and `UploadFolder` already included `'avatars'` in its union. Building a second near-identical uploader would have been pure duplication of a component designed to be generic from the start.

**Same smart/presentational split as `RestaurantForm`/`RestaurantFormPage`.** `ProfileForm` control names stay snake_case (`email`, `first_name`, `last_name`, `phone`) matching `UserProfileUpdate`'s DTO keys for `applyFieldErrors`; `username`/`role` are read-only on this endpoint per `docs/API.md` and render as plain text, never controls. `avatar` is not a form control — tracked as a plain `avatarUrl` input the parent (`ProfilePage`) keeps live, merged into the emitted value at submit, identical to `cover_image`'s treatment in `RestaurantForm`. `ProfilePage` owns the Cloudinary upload (folder `'avatars'`) and stays on the page after a successful save — no navigation, the form simply reflects the now-updated `user()` signal.

**No E2E coverage of a field-level server error's visible rendering — because nothing in this codebase renders one yet.** `apiErrorMessage()` deliberately returns `undefined` for `type: 'field'` errors (its own doc comment: those belong on their controls, not a banner), but no form template anywhere (`RestaurantForm`, `MenuItemForm`, now `ProfileForm`) actually wires a `<mat-error>` to display it — confirmed by grepping the whole codebase for `mat-error`, zero hits. The real logic this commit adds — a field error landing on the correct `FormControl` via `applyFieldErrors` — is exactly what `ProfileForm.spec.ts`'s field-error case checks. An E2E assertion on visible error text would be testing behavior that doesn't exist, not filling a gap this commit introduced.

No unit spec for `ProfilePage` — thin orchestration only (same reasoning as `RestaurantFormPage`/`MenuManagerPage`), real coverage from `e2e/specs/profile.spec.ts` (view current values, edit + avatar upload asserted against the real captured PATCH body).

## SEO

Scoped to the two real public/server-rendered pages this app has: `/restaurants` and `/restaurants/:id`. PLAN.md's original route table also lists `/`, `/categories`, `/categories/:slug` — none were ever built (`CLAUDE.md`'s documented gap: no `path: ''` route exists) — out of scope, same as a 404 page.

**`environment.siteUrl`, threaded exactly like `apiBaseUrl`** (`NG_APP_SITE_URL`/`SITE_URL`, same `scripts/build.mjs`/`.env.example`/`environment.prod.ts` plumbing). Canonical URLs, hreflang alternates, JSON-LD and the sitemap all need an absolute origin, and there's no fixed prod domain committed anywhere (Render assigns it at deploy time). `server.ts` imports the same `environment.siteUrl` rather than a second env var — it's built through the same server bundle and already resolves `@environments/*`/`@core/*`.

**`stripLocalePrefix()` (`core/utils/locale-path.ts`) extracted from `LanguageSwitcher`**, reused by `SeoService`. `LanguageSwitcher` already solved "current path without its locale segment" for the alternate-language link; hreflang needs exactly the same value.

**`SeoService`/`JsonLdService` (`core/services/seo/`)** wrap `Meta`/`Title` (both `providedIn: 'root'`, no new provider needed) plus manual `<link>`/`<script>` management via `DOCUMENT` — Angular has no built-in for canonical/hreflang `<link>` tags or a JSON-LD `<script>`. Both upsert by a `data-seo` marker attribute instead of appending, so repeated calls (a filter change, or client-side navigation between two restaurant pages) never accumulate duplicate tags.

- `SeoService.updatePage({ title, description, canonicalQueryParams? })` sets `<title>`, `<meta name="description">`, Open Graph tags, and three `<link>`s: canonical (current locale), `hreflang="es"`/`"en"`, and `hreflang="x-default"` (pointing at the `es` URL, matching `server.ts`'s own `DEFAULT_LOCALE`). Canonical/hreflang are built from `DOCUMENT.location.pathname/search` by default — the same source `LanguageSwitcher` already trusts in SSR. `canonicalQueryParams` (only used by `RestaurantListPage`) replaces the raw query string with a rebuilt one (sorted, `undefined`/`''` dropped), letting the page normalize `page=1` out of its canonical (identical content to no `page` param) while keeping other filters self-canonical — each filter combination is meant to be its own crawlable URL, per that page's own existing doc comment.
- `JsonLdService.set(schema)`/`.clear()` manage exactly one `<script type="application/ld+json">` tag. No generic multi-schema registry — only `RestaurantDetailPage` emits JSON-LD today.

**`buildRestaurantJsonLd()` (`core/utils/restaurant-json-ld.ts`) is a pure function**, not inline in the page's `effect()` — the actual mapping/branching logic is what's worth a unit test, not the wiring. `geo` (`GeoCoordinates`) only when both `latitude`/`longitude` aren't `null` — the same condition `RestaurantHero.mapsUrl()` already branches on, for a different consumer. `aggregateRating` only when `totalReviews > 0` — an empty rating block is worse than none, per Google's own structured-data guidance. `openingHoursSpecification` only for days actually present in `openingHours`. `priceRange` needs no translation table: this API's `$`/`$$`/`$$$`/`$$$$` scale is already a valid schema.org value.

**`sitemap.xml`/`robots.txt` are dynamic Express routes in `server.ts`, not static `public/` files** — both need `environment.siteUrl`, which a static file can't vary by environment, and are registered before the locale-redirect middleware (neither path is locale-prefixed). `sitemap.xml` loops `restaurants/?page=N&page_size=100` (Node's global `fetch()`) following `next` until it's `null` — no silent cap — then calls the pure `buildSitemapXml()` builder (`core/utils/sitemap-builder.ts`): a flat `<url>` list covering `/restaurants` and every `/restaurants/{id}` for each locale, no `xhtml:link` hreflang annotations (valid either way per the sitemap protocol; each page already emits its own hreflang `<link>`s). **On a backend fetch failure, the route degrades to just the two `/restaurants` locale entries rather than a 500** — verified for real during this commit's own manual testing: the live dev backend returned a genuine `429` mid-verification, and the route degraded exactly as designed instead of breaking.

No E2E spec for this commit. `sitemap.xml`/`robots.txt` are Express routes outside `playwright.config.ts`'s webServer's meaningful reach, and `sitemap.xml` specifically needs a live backend the mocked default suite deliberately never runs against — verified manually instead via `curl` against a real production build + real backend (confirmed `<link rel="canonical">`/`hreflang` alternates/the JSON-LD `<script>` all present in the raw server-rendered HTML for both a restaurant detail page and a filtered listing page). The real logic — locale stripping, JSON-LD mapping, sitemap XML, canonical/hreflang URL construction — has real unit tests instead.

## Live E2E smoke suite

`e2e/specs/live-smoke.spec.ts`, tagged `@live`, no `page.route()` mocking — every request hits the real backend at `API_BASE_URL`. One test: log in, browse to a restaurant, round-trip a favorite toggle. Its job is catching contract drift against the real API (a renamed field, a moved endpoint, a changed status code), not coverage — the mocked default suite already covers behavior and branching in detail. Kept to one test deliberately: this API throttles anonymous clients at 30 req/min.

**One `playwright.config.ts`, two projects, selected by tag rather than a second `testDir`.** `chromium` (`npm run e2e`) runs everything except `@live`-tagged tests (`grepInvert: /@live/`); `live` (`npm run e2e:live`) runs only `@live`-tagged tests (`grep: /@live/`), with `retries: 0` — retrying a failure against an already-throttled shared backend would make the problem worse, not recover from it. Both share the same `webServer`: the production build's `apiBaseUrl` is baked in from `.env`'s `API_BASE_URL`, already `http://localhost:8000/api` locally, so no separate build or server config is needed for the live project — the only difference between the two suites is whether a spec registers `page.route()` mocks.

**Real credentials via `E2E_TEST_USERNAME`/`E2E_TEST_PASSWORD`, never committed.** `live-smoke.spec.ts` skips itself with a clear reason (not a failure) when these are unset — this suite depends on external state (a running backend, a seeded/real account) the repo itself can't supply. `playwright.config.ts` now loads `.env` at startup (same guarded `existsSync` + `process.loadEnvFile` pattern as `scripts/build.mjs`) so the live spec's own process can read them — the mocked suite's `webServer` already loads `.env` too, but only inside the `npm run build` child process it spawns, which doesn't reach the test-runner process itself.

Verified for real: registered a throwaway account against the live dev backend, ran `npm run e2e:live` against it, green on the first real attempt — including hitting the same anonymous throttle mid-verification that this suite's own doc comment warns about.

## Form validation errors

**Found via manual testing, not planned as a commit:** submitting `RestaurantForm` blank showed nothing — no error text anywhere. Root cause: the submit button was `[disabled]="form.invalid || ..."` (a disabled button swallows a click, so nothing could ever reveal _why_), and separately **this app had zero `<mat-error>` elements anywhere**, confirmed by a full-codebase grep while building the profile page (commit 17) — a systemic gap across every form, not specific to `RestaurantForm`. Fixed everywhere at once: `RestaurantForm`, `MenuItemForm`, `ProfileForm`, `ReviewForm`, `LoginPage`, `RegisterPage`.

**The fix is a clickable button plus revealed errors, not a smarter disabled state.** Every submit button's `[disabled]` binding now only reflects pending/uploading state, never `form.invalid`; every `submit()` calls `this.form.markAllAsTouched()` before its existing `if (form.invalid) { return; }` guard (unchanged — an invalid submit still never calls the API). Since Material's default `ErrorStateMatcher` shows a control's `<mat-error>` once it's `invalid && touched`, marking everything touched on a blocked submit attempt reveals every missing field at once, not just ones the user happened to blur through one at a time. One existing E2E assertion changed as a direct, intentional consequence: `auth.spec.ts`'s password-mismatch test no longer expects the Create-account button disabled (it's always clickable now) — it clicks it and asserts the mismatch message still shows and the API still isn't called, a strictly better proof (the guard, not an unclickable button, is what's actually preventing the request).

**`core/utils/field-error-message.ts`** (`fieldErrorMessage(control)`) is the one place that maps a control's error state to display text — mirrors the existing `apiErrorMessage()` convention exactly (same file location, same "exposed as a `protected readonly` class member for template use" idiom already used everywhere), avoiding the same `@if` chain repeated by hand in ~20 places across six forms. Precedence: a backend `server` error (set by `applyFieldErrors`) wins over a client-side one, since it's already the most specific worded message available; otherwise `required`/`email`/`min` map to a generic, reusable message each. Returns `undefined` (nothing rendered) for a valid or not-yet-touched control. `applyFieldErrors` (`core/utils/apply-field-errors.ts`) now also calls `control.markAsTouched()` alongside `setErrors()` — a backend-rejected field must display immediately, since the form was client-valid when submitted and nothing would otherwise have touched it yet.

**`RestaurantForm`'s required-field set was wrong** — only `name` had `Validators.required`, but the backend also requires `address`/`city`/`country` (caught during manual testing, not documented anywhere in `docs/API.md`'s field list). Added to match. `state`/`postal_code`/`phone`/`email`/`website`/`price_range` stay optional.

## Home page

`/` (`HomePage`), `RenderMode.Prerender`, no guard — the route this app's route table always specified but never had: before this, `/` had no registered client route at all, so the production build 404'd on `/es/`/`/en/` (confirmed via `curl`) and `npm run build`'s log read "Prerendered 0 static routes." Deliberately small scope: a static hero (headline, tagline, a `routerLink="/restaurants"` CTA styled `mat-flat-button` — the same anchor-as-button pattern `MyRestaurantsPage`'s "Add restaurant" link uses) plus one below-the-fold section previewing 6 restaurants via `RESTAURANT_DATA.list(signal({ pageSize: 6 }))`, reusing the already-built, already-tested `RestaurantGrid` (same reasoning `FavoritesPage` uses to justify reuse over a new grid). No invented sort ("top-rated", "popular") — `docs/API.md`'s `restaurants/` query params don't support one, so this is honestly just the first page, default order. `SeoService.updatePage()` is called once in the constructor, not wrapped in `effect()` — unlike every other SEO-emitting page, nothing here ever changes after construction, so there's nothing to react to.

**A real finding, not just a plan detail: because the whole page is `Prerender`, the restaurant preview is baked into the static HTML at build time, same as the hero — it does not refetch per visitor.** Confirmed via `curl` against a real production build: three real, named restaurants with real image URLs sit directly in the raw HTML response, not a loading skeleton. This means the preview goes stale between deploys, refreshing only on the next `npm run build`. Kept deliberately, not engineered around — forcing a genuine client-only fetch would mean shipping a skeleton to every crawler and first-paint visitor (or abandoning `Prerender` for this page entirely), both of which work against the actual point of this commit: real, crawlable content, and demonstrating `Prerender` itself. A stale-until-next-deploy preview is a fine tradeoff for a portfolio project's landing page — arguably better for SEO than a client-only fetch would have been.

`core/utils/sitemap-builder.ts` now also emits `/{locale}` (the root) alongside `/{locale}/restaurants` and each restaurant detail URL — confirmed via `curl http://localhost:4200/sitemap.xml` after this commit.

No unit spec for `HomePage` — static content plus a thin, already-tested-elsewhere data fetch, same as every other thin orchestration page in this codebase. New `e2e/specs/home.spec.ts` **finally lands the locale-redirect/language-switcher coverage this project's own testing-strategy plan flagged as blocked since commit 10** (no home route existed to test against): `/` redirects to `/es/`, the hero/CTA render, the CTA and "see all" link both navigate to `/restaurants`, the preview renders from a mocked response, and the language switcher swaps `/en/` ↔ `/es/` while staying on the home page.
