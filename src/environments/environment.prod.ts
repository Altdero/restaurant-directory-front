import { Environment } from './environment.model';

export const environment: Environment = {
  production: true,
  apiBaseUrl: NG_APP_API_BASE_URL,
  siteUrl: NG_APP_SITE_URL,
  dataLayer: 'http-resource',
};
